// config.js
// Environment configuration for EcoScan application

(function() {
    // Environment variables
    const envConfig = {
        // Your Perplexity API key
        PERPLEXITY_API_KEY: ''
    };
    
    // Make configuration available globally
    window.ENV = envConfig;
})();
